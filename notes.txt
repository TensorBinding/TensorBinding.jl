Some notes and todo's on the package:

*   Make it installable with versions etc
*   Update the LDOS with MPO-MPS method (later possibly also k if it works with Hamiltonian) DONE
*   Include excitons as a 2 particle class Hamiltonian
*   I think we need to separate geometry into the regular geometries (chain,sq,tri,hex)
    and then a class of custom hamiltonians
*   I added a printinfo on the KPM_tn cause it was annoying to see all the moments all the time
*   Need to replace mpstompo as it uses Quantics_asdiagonal while we have a general function

Questions:
*   aux index can be in begin or end for both 1D and 2D? 
*   what is get_PH_from_Tn?


Package version dependencies:

*   Manifest.toml was generated with Julia 1.11.2 but people will probably be running 1.12.x.
    This gave me a JLLwrappers issue. This ties up with the above statement of making this official package
    We probably don't want to have people install a local project and manifest
    Local workaround is to run " julia --project=. -e "using Pkg; Pkg.update()" " to update Manifest.toml
*   We need to resolve issues with package dependencies: delete old stuff etc... 
*   TCIITensorConversion seems to always give issues. 


Notes for myself on what I need to do still to understand the full package:

*   Understand the caching in Hamiltonian Constructors
*   Learn all the possibilities of what incremental objects can be added and the logic behind
*   Understand how spin, nambu, layer dofs are encoded in the additional core: logic inside the core
*   Check how center kw works in Ham
*   Check what _tn_cache and _tn_Ncheb and _density_cache are doing (probably for MF stuff?)
*   In general why this "cache" structure and what is the computing logic? (ask chat to explain stuff)

